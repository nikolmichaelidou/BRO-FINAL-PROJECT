<div class=    <div class="actions">
      <button class="btn-icon"><i class="fa-regular fa-heart"></i></button>
      <button class="btn-icon filled"><i class="fa-solid fa-shopping-bag"></i></button>
    </div>duct-card">
  <div class="product-card__image">
    <img src="{{ product.image }}" alt="{{ product.title }}">
    {% if product.badge %}
      <div class="product-card__badge product-card__badge--{{ product.badge | lower | replace(' ', '-') }}">{{ product.badge }}</div>
    {% endif %}
  </div>
  <div class="product-card__info">
    <h4 class="product-card__name">{{ product.title }}</h4>
    <p class="category">{{ product.category }}</p>
    <p class="product-card__price">{{ product.price }}</p>
    <div class="actions">
      <button class="btn-icon">â™¡</button>
      <button class="btn-icon filled">ðŸ›ï¸</button>
    </div>
  </div>
</div>

